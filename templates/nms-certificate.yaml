apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name }}-nms-certificate
spec:
  secretName: "{{ .Release.Name }}-{{ .Values.nms.secretName }}"
  commonName: "*.{{ .Values.dnsDomain }}"
  subject:
    countries:
      - US
  duration: {{ .Values.nms.duration }}
  issuerRef:
    name: {{ .Release.Name }}-root-issuer
  dnsNames:
  - "*.{{ .Values.dnsDomain }}"
  - "*.nms.{{ .Values.dnsDomain }}"
